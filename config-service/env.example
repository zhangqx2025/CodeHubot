# ==========================================
# CodeHubot 配置服务环境变量配置
# 使用前请复制此文件为 .env 并修改配置
# ==========================================

# ==================== 数据库配置 ====================
# 注意：使用 aiot_device 数据库，不是 aiot_admin
# 数据库主机地址
DB_HOST=localhost

# 数据库端口（默认 3306）
DB_PORT=3306

# 数据库用户名
DB_USER=aiot_user

# 数据库密码
DB_PASSWORD=your_secure_password

# 数据库名称（注意：使用 aiot_device 数据库）
DB_NAME=aiot_device

# ==================== MQTT配置 ====================
# MQTT服务器地址（设备将连接到这里）
MQTT_BROKER=localhost

# MQTT服务器端口
MQTT_PORT=1883

# 是否使用SSL（通常为false）
MQTT_USE_SSL=false

# ==================== API服务器配置 ====================
# 主API服务器地址（后端服务地址）
API_SERVER=http://localhost:8000

# OTA固件服务器地址（通常与API_SERVER相同）
OTA_SERVER=http://localhost:8000

# ==================== 服务配置 ====================
# 服务监听端口
PORT=8001

# ==================== 速率限制配置 ====================
# 每个IP在时间窗口内的最大请求数
RATE_LIMIT_REQUESTS=10

# 时间窗口（秒）
RATE_LIMIT_WINDOW=60

# ==================== 日志配置 ====================
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
# 注意：当前代码中未使用此配置，日志级别为硬编码的INFO
LOG_LEVEL=INFO
