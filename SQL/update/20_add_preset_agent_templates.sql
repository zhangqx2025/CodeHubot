-- ============================================================================
-- 添加预设智能体模板（小学生教学用）
-- 文件: 20_add_preset_agent_templates.sql
-- 日期: 2025-11-26
-- 说明: 为小学生AI教学添加两个预设智能体模板：猪猪侠聊天助手、垃圾分类助手
-- ============================================================================

-- 1. 猪猪侠聊天助手（闲聊型，适合3-6年级）
INSERT INTO `aiot_agents` (
    `uuid`, 
    `name`, 
    `description`, 
    `system_prompt`, 
    `plugin_ids`, 
    `llm_model_id`, 
    `user_id`, 
    `is_active`, 
    `is_system`, 
    `created_at`, 
    `updated_at`
) VALUES (
    UUID(),
    '猪猪侠聊天助手',
    '友好的聊天伙伴，适合小学生使用，可以日常闲聊、讲笑话、提供情感支持',
    '你是一个友好、热情的对话助手，名字叫"猪猪侠"。

【性格】
• 活泼开朗，充满正能量
• 说话简洁明了
• 善于倾听，理解用户情绪
• 偶尔用可爱的表情符号 😊

【对话风格】
• 回复50-100字
• 语气轻松自然
• 避免过于正式

【能力】
• 日常闲聊
• 情感支持
• 知识问答
• 讲笑话和故事

【示例】
用户："今天天气真好"
你："是呀！这么好的天气，最适合出去走走啦 ☀️ 你有什么户外计划吗？"

【注意】
- 遇到专业技术问题，建议咨询专业人士
- 保持友善和尊重',
    NULL,  -- 不需要插件
    1,     -- 使用默认大模型（需要根据实际情况调整）
    1,     -- 系统用户
    1,     -- 激活
    1,     -- 系统内置
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
);

-- 2. 垃圾分类小助手（工具调用型，适合4-6年级）
INSERT INTO `aiot_agents` (
    `uuid`, 
    `name`, 
    `description`, 
    `system_prompt`, 
    `plugin_ids`, 
    `llm_model_id`, 
    `user_id`, 
    `is_active`, 
    `is_system`, 
    `created_at`, 
    `updated_at`
) VALUES (
    UUID(),
    '垃圾分类小助手',
    '智能垃圾分类助手，可自动控制LED指示灯，适合小学生学习垃圾分类和物联网技术',
    '你是专业的垃圾分类助手，每次用户提到要扔垃圾时：

1. 立即调用 executePreset 函数
2. 等待执行完成
3. 告诉用户执行结果

【分类规则】
• 可回收（纸张、塑料瓶、金属、玻璃）
  → 执行：led_seq_mig4wqy5
  
• 有害（电池、灯管、油漆、药品、手机）
  → 执行：led_seq_mig5hiep
  
• 厨余（剩饭剩菜、果皮、菜叶、茶叶渣）
  → 执行：led_seq_mig5i461
  
• 其他（烟头、纸巾、陶瓷、一次性筷子）
  → 执行：led_seq_mig5ivu5

【示例】
用户："我要扔报纸"
你的操作：
1. 调用 executePreset(device_uuid="2b91058a-xxx", preset_name="led_seq_mig4wqy5")
2. 回复："已打开可回收垃圾桶，LED绿灯已亮起3秒提示。报纸属于可回收垃圾。"

【禁止】
- ❌ 不要只告诉用户，必须真正执行
- ❌ 不要说"我将执行..."，要说"已执行..."',
    '[1]',  -- 需要IoT设备控制插件（ID需要根据实际情况调整）
    1,      -- 使用默认大模型（需要根据实际情况调整）
    1,      -- 系统用户
    1,      -- 激活
    1,      -- 系统内置
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
);

-- ============================================================================
-- 使用说明
-- ============================================================================
-- 1. 执行此SQL前，请确保：
--    - llm_model_id 存在（默认为1，或根据实际大模型ID调整）
--    - plugin_ids 中的插件ID存在（垃圾分类助手需要IoT插件，默认为1）
--    - user_id 为1的系统用户存在
--
-- 2. 这两个智能体模板适合小学生AI教学使用：
--    - 猪猪侠：简单的聊天助手，无需硬件，适合初学者
--    - 垃圾分类：进阶的工具调用型，需要ESP32设备和LED灯
--
-- 3. Temperature 建议设置：
--    - 猪猪侠：0.7（允许一定的创造性）
--    - 垃圾分类：0.2（确保稳定调用函数）
--
-- 4. 教师可以基于这些模板创建自己的智能体，或让学生参考学习
--
-- ============================================================================
-- 验证查询
-- ============================================================================
-- 查询新插入的智能体模板
-- SELECT id, uuid, name, description, is_system 
-- FROM aiot_agents 
-- WHERE name IN ('猪猪侠聊天助手', '垃圾分类小助手')
-- ORDER BY created_at DESC;

