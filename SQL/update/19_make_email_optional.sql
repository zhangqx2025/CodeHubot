-- ============================================================
-- 数据库更新脚本 - 19
-- 日期: 2025-11-26
-- 说明: 将用户邮箱字段改为可选
-- ============================================================

USE aiot;

-- 修改 aiot_core_users 表，使 email 字段可为 NULL
ALTER TABLE aiot_core_users 
MODIFY COLUMN email VARCHAR(255) NULL COMMENT '用户邮箱（可选）';

-- 注意：现有的 UNIQUE 约束和索引保持不变
-- 邮箱仍然唯一，但可以为空

-- 查看修改后的表结构
DESCRIBE aiot_core_users;

-- ============================================================
-- 说明：
-- 1. email 字段从 NOT NULL 改为 NULL
-- 2. 保留 UNIQUE 约束（邮箱仍需唯一，但可以为空）
-- 3. 保留索引（提高查询性能）
-- 4. 不影响现有数据
-- ============================================================

