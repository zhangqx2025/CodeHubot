# ==========================================
# CodeHubot 插件服务环境变量配置
# 使用前请复制此文件为 .env 并修改配置
# ==========================================

# ==================== 服务配置 ====================
# 服务监听端口
PORT=9000

# 服务主机地址（0.0.0.0表示允许外部访问）
HOST=0.0.0.0

# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 是否启用自动重载（开发环境建议true，生产环境建议false）
RELOAD=false

# ==================== 后端服务配置 ====================
# 插件后端服务地址（推荐使用，避免循环依赖）
# ⚠️ 重要：plugin-backend-service 必须先启动
# 端口 9002（9001 被 MQTT WebSocket 占用）
PLUGIN_BACKEND_URL=http://localhost:9002


# ==================== 安全配置 ====================
# 是否启用CORS（跨域资源共享）
CORS_ENABLED=true

# 允许的来源（多个用逗号分隔，*表示允许所有）
# 生产环境建议设置为具体域名，例如：
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com
CORS_ORIGINS=*

# ==================== 其他配置 ====================
# 请求超时时间（秒）
REQUEST_TIMEOUT=30

# 是否在响应中包含详细错误信息（生产环境建议false）
DEBUG_MODE=false
