# AIOT 外部插件服务环境变量配置
# 使用前请复制此文件为 .env 并修改配置

# ==================== 服务配置 ====================
# 服务监听端口
PORT=9000

# 服务主机地址（0.0.0.0表示允许外部访问）
HOST=0.0.0.0

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 是否启用自动重载（开发环境建议true，生产环境建议false）
RELOAD=true

# ==================== 后端服务配置 ====================
# 后端API服务地址
BACKEND_URL=http://localhost:8000

# 后端内部API密钥（必须配置！用于plugin-service调用后端API）
# ⚠️ 重要：必须与后端.env中的INTERNAL_API_KEY保持一致
# 生成强密钥的方法：
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#   或
#   openssl rand -hex 32
BACKEND_API_KEY=your-internal-api-key-change-me

# ==================== 安全配置 ====================
# 是否启用CORS（跨域资源共享）
CORS_ENABLED=true

# 允许的来源（多个用逗号分隔，*表示允许所有）
# 生产环境建议设置为具体域名，例如：
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com
CORS_ORIGINS=*

# ==================== 其他配置 ====================
# 请求超时时间（秒）
REQUEST_TIMEOUT=30

# 是否在响应中包含详细错误信息（生产环境建议false）
DEBUG_MODE=true

