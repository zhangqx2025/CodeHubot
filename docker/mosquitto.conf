# Mosquitto MQTT Broker 最简配置
# 适用于生产环境（允许匿名访问）

# 监听端口
listener 1883
allow_anonymous true

# 持久化配置
persistence true
persistence_location /mosquitto/data/

# 日志配置
log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
