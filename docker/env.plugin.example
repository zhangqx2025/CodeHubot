# ==========================================
# CodeHubot Docker Compose 环境变量配置
# 包含 plugin-backend-service
# 
# 使用方法：
# 1. 复制此文件为 .env
#    cp env.plugin.example .env
# 2. 修改配置项
# 3. 运行 docker-compose -f docker-compose.plugin.yml up -d
# ==========================================

# ==================== 数据库配置 ====================
MYSQL_DATABASE=aiot
MYSQL_USER=aiot_user
MYSQL_PASSWORD=change_this_password
MYSQL_ROOT_PASSWORD=change_this_root_password
MYSQL_PORT=3306

# ==================== Redis配置 ====================
REDIS_PASSWORD=redis_password
REDIS_PORT=6379

# ==================== 后端配置 ====================
# JWT 密钥（至少32个字符）
# 生成方法：python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-at-least-32-characters

# 服务器基础URL（用于生成链接）
SERVER_BASE_URL=http://localhost:8000

# 后端服务端口
BACKEND_PORT=8000

# ==================== MQTT配置 ====================
# MQTT用户名（可选，留空表示匿名访问）
MQTT_USERNAME=

# MQTT密码（可选，留空表示匿名访问）
MQTT_PASSWORD=

# ==================== 插件服务配置 ====================
# Plugin Backend Service 端口（直接访问数据库和MQTT）
PLUGIN_BACKEND_PORT=9001

# Plugin Service 端口（对外接口）
PLUGIN_SERVICE_PORT=9000

# ==================== 前端配置 ====================
# 前端API基础URL
VITE_API_BASE_URL=http://localhost:8000

# 前端服务端口
FRONTEND_PORT=80

# ==================== 其他配置 ====================
# 日志级别：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 环境类型：development, production
ENVIRONMENT=production

