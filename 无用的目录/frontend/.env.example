# ==========================================
# 前端应用环境变量配置示例 - 公共配置
# CodeHubot 物联网设备服务系统
# ==========================================
# 
# 使用方法：
# 1. 复制此文件为 .env（公共配置，所有环境都会加载）
# 2. 复制 .env.development.example 为 .env.development（开发环境特定配置）
# 3. 复制 .env.production.example 为 .env.production（生产环境特定配置）
# 4. 根据实际情况修改配置值
#
# Vite 环境变量加载顺序（优先级从低到高）：
# 1. .env                    # 所有环境都会加载（公共配置）
# 2. .env.local              # 所有环境都会加载（本地覆盖，会被 git 忽略）
# 3. .env.[mode]             # 只在指定模式下加载（如 .env.development）
# 4. .env.[mode].local        # 只在指定模式下加载（本地覆盖，会被 git 忽略）
#
# 注意：所有环境变量必须以 VITE_ 开头才能在代码中访问
# ==========================================

# ==================== 公共配置 ====================
# 以下配置在所有环境中都会生效，除非在环境特定文件中被覆盖

# 应用标题
# 在 src/main.js 中用于设置页面标题（document.title）
# 在用户下拉菜单的"关于系统"中显示
VITE_APP_TITLE=CodeHubot 物联网设备管理系统

# 应用版本
# 在用户下拉菜单的"关于系统"中显示
VITE_APP_VERSION=1.0.0

# 请求超时时间（毫秒）
# 在 src/api/request.js 中使用
VITE_API_TIMEOUT=10000

# WebSocket 地址（如果使用 WebSocket 实时通信）
# VITE_WS_URL=ws://localhost:8000/ws

# MQTT WebSocket 地址（如果前端需要直接连接 MQTT）
# VITE_MQTT_WS_URL=ws://localhost:9001
