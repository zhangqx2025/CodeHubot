# ==========================================
# 前端应用环境变量配置示例 - 生产环境
# CodeHubot 物联网设备服务系统
# ==========================================
# 
# 使用方法：
# 1. 复制此文件为 .env.production
# 2. 根据实际情况修改配置值
# 3. 构建生产版本时会自动加载此文件
#
# 注意：此文件会覆盖 .env 中的同名配置
# ==========================================

# ==================== 生产环境配置 ====================
# API 基础地址
# 生产环境使用相对路径 /api，通过 Nginx 反向代理到后端服务
# Nginx 配置会将 /api 请求代理到 http://127.0.0.1:8000
# 前后端使用同一域名，避免跨域问题
VITE_API_BASE_URL=/api

# 是否启用调试模式（生产环境建议设为 false）
VITE_DEBUG_MODE=false

# ==================== 说明 ====================
# 1. 生产环境使用 Nginx 转发 API，前后端使用同一域名
# 2. Nginx 配置示例（参考 deploy/docs/deployment-guide.md）：
#    location /api {
#        proxy_pass http://127.0.0.1:8000;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
# 3. 如果后端独立部署在不同域名，才需要设置为完整地址（不推荐）：
#    VITE_API_BASE_URL=https://api.your-domain.com
